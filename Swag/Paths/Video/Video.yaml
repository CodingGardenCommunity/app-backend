# GET /video
AllVideos:
  tags:
    - Video
  summary: Returns list of Videos.
  operationId: getAllVideo
  description: This end point returns list of Videos that CodingGarden Community has. # Suggestions?
  responses:
    200:
      $ref: 'Video.responses.yaml#/AllVideos/200'
    500:
      $ref: 'Video.responses.yaml#/AllVideos/500'

# GET /video/{id}
SingleVideo:
    tags:
      - Video
    summary: Returns single video object
    operationId: getSingleVideoById
    description: This end point returns single video of CodingGarden Community based on `id`. # Suggestions?
    parameters:
      - name: id
        in: path
        required: true
        description: Video `id`.
        schema:
          type: string
          example: 5d53a0cfbb779e2988d67d22
    responses:
      200:
        $ref: 'Video.responses.yaml#/SingleVideo/200'
      404:
        $ref: 'Video.responses.yaml#/SingleVideo/404'
      500:
        $ref: 'Video.responses.yaml#/SingleVideo/500'

