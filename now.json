{
  "name": "app-backend",
  "version": 2,
  "builds": [{
    "src": "src/index.js",
    "use": "@now/node-server"
  }],
  "routes": [{
    "src": "/(.*)", "dest": "src/index.js"
  }],
  "env": {
    "NODE_ENV": "${NODE_ENV}",
    "MONGO_URI": "${MONGO_URI}"
  }
}